<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.librarymanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OpeningLibraryManagementApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginStudentActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".StudentHomePageActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".TeacherLogInActivity" />
        <activity android:name=".TeacherHomePage" />
        <activity android:name=".DisplayAllStudentInformation" />
        <activity android:name=".DeleteAnyInformationActivity" />
        <activity android:name=".AddBookActivity" />
        <activity android:name=".ViewAllBookItem"></activity>
    </application>

</manifest>